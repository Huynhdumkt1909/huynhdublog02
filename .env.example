# TinaCMS Cloud (optional) — chỉ cần khi muốn auth/media qua Tina Cloud thay vì chỉnh sửa local.
# Để trống thì `npm run cms` vẫn chạy ở chế độ local, lưu thẳng vào git.
TINA_CLIENT_ID=
TINA_TOKEN=

# GTM container ID, ví dụ GTM-XXXXXXX — điền vào src/site.config.ts sau khi tạo container
